## 🚀 Setup Locale (5 minuti)

### 1️⃣ Clone Repository
```bash
git clone git@github.com:dataxys/crime_economy_italy_analysis.git
cd crime_economy_italy_analysis
```
 Oppure
```bash
scarica la cartella TOTALE zip/tar/ da "Code" in alto a destra
```
### 2️⃣ Create Virtual Environment (per avere tutti le stesse librerie)
**Mac/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```
**Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```
### 3️⃣ Open in VSCode
```bash
code . 
[apre Visual Studio Code con la cartella aperta automaticamente se correttamente installato]
```
**Installa estensioni VSCode:**
- Python
- Jupyter
- GitLens (opzionale)
### 4️⃣ Run Notebooks
1. Apri `notebooks/01_data_loading.ipynb`
2. Seleziona kernel: venv Python
3. Run All Cells ▶️
